% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/iterative_removing.R
\name{check_all_criteria}
\alias{check_all_criteria}
\title{Internal: unified evaluation (revised)}
\usage{
check_all_criteria(
  data,
  model_configs,
  var_list = NULL,
  criteria,
  verbose = TRUE
)
}
\arguments{
\item{data}{data.frame of observed variables (may contain `..row_id`).}

\item{model_configs}{named list; each element has `model` (lavaan syntax),
optional `key_paths` (labeled paths), optional `check_fit` (logical,
default TRUE).}

\item{var_list}{optional named list of measurement models.}

\item{criteria}{list of thresholds/flags (see [iterative_removing()]).}

\item{verbose}{logical; print a report.}
}
\value{
list with sublists `reliability`, `paths`, `fit`, `cmb`, each with
  `pass` (logical or NA when not checked) and `details`.
}
\description{
Evaluates four criteria: reliability (per construct in `var_list`), key
path significance, fit indices, and common method bias. Each model in
`model_configs` is fitted ONCE and the fit object is reused for both the
path and the fit checks. A model with `check_fit = FALSE`, or a saturated
model (df = 0), is excluded from the fit check.
}
\keyword{internal}
