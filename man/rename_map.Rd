% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rename_map.R
\name{rename_map}
\alias{rename_map}
\title{Bulk-rename Columns}
\usage{
rename_map(data, map)
}
\arguments{
\item{data}{A \code{data.frame} or tibble whose column names will be modified.}

\item{map}{A named character vector: use \strong{new_name = "old_name"} (old name should be quoted)}
}
\value{
A \code{data.frame} (or tibble) with columns renamed according to \code{map}.
}
\description{
Rename columns in a \code{data.frame} or tibble by replacing specified old prefixes
with new prefixes.  Only column names that start with an \code{old} prefix will be
modified; all other names remain unchanged.
}
\examples{
# Create a sample data.frame
df <- data.frame(
  measureA1 = rnorm(5),
  measureA2 = rnorm(5),
  factorX1  = runif(5),
  factorX2  = runif(5),
  Timestamp = Sys.time() + 1:5,
  stringsAsFactors = FALSE
)

# Define mapping: old → new prefixes
prefix_map <- c(
  mA = "measureA",
  fX = "factorX"
)

# Apply rename_map()
df_renamed <- rename_map(df, prefix_map)
names(df_renamed)
#> [1] "mA1"      "mA2"      "fX1"      "fX2"      "Timestamp"

}
