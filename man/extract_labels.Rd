% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_labels.R
\name{extract_labels}
\alias{extract_labels}
\title{Extract Variable Labels from a Data Frame}
\usage{
extract_labels(data)
}
\arguments{
\item{df}{A data frame (or tibble) whose columns have \code{label} attributes.}
}
\value{
A tibble with two columns: \code{variable} (the column name) and \code{label} (the label value).
}
\description{
This function retrieves the variable labels from each column of a data frame.
It expects that each column of the data frame has a "label" attribute (as used in imported datasets or assigned manually).
}
\examples{
# Create a sample data frame with labels
df <- data.frame(id = 1:3, score = c(5, 4, 8))
attr(df$id, "label") <- "ID Number"
attr(df$score, "label") <- "Score"
extract_labels(df)

}
